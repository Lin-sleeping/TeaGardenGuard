<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.IotHumidityMapper">
    <insert id="insertIotHumidity" parameterType="com.ruoyi.system.domain.IotHumidity">
        INSERT INTO iot_humidity (value, data_time)
        VALUES (#{value}, #{dataTime})
    </insert>
    <select id="selectIotHumidityListByTime" resultType="com.ruoyi.system.domain.IotHumidity">
        SELECT * FROM iot_humidity
        WHERE data_time BETWEEN #{startTime} AND #{endTime}
        ORDER BY data_time ASC
    </select>
    <select id="selectLatestIotHumidity" resultType="com.ruoyi.system.domain.IotHumidity">
        SELECT * FROM iot_humidity ORDER BY data_time DESC LIMIT 1
    </select>
    <select id="selectIotHumidityListMapByTime" resultType="java.util.HashMap">
        SELECT value, DATE_FORMAT(data_time, '%H:%i:%s') as time
        FROM iot_humidity
        WHERE data_time BETWEEN #{startTime} AND #{endTime}
        ORDER BY data_time ASC
    </select>
    <select id="selectLatestIotHumidityMap" resultType="java.util.HashMap">
        SELECT value, DATE_FORMAT(data_time, '%H:%i:%s') as time
        FROM iot_humidity
        ORDER BY data_time DESC
        LIMIT 1
    </select>
</mapper> 