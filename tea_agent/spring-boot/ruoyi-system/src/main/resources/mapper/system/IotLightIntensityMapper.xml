<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.IotLightIntensityMapper">
    <insert id="insertIotLightIntensity" parameterType="com.ruoyi.system.domain.IotLightIntensity">
        INSERT INTO iot_light_intensity (value, data_time)
        VALUES (#{value}, #{dataTime})
    </insert>
    <select id="selectIotLightIntensityListByTime" resultType="com.ruoyi.system.domain.IotLightIntensity">
        SELECT * FROM iot_light_intensity
        WHERE data_time BETWEEN #{startTime} AND #{endTime}
        ORDER BY data_time ASC
    </select>
    <select id="selectLatestIotLightIntensity" resultType="com.ruoyi.system.domain.IotLightIntensity">
        SELECT * FROM iot_light_intensity ORDER BY data_time DESC LIMIT 1
    </select>
    <select id="selectIotLightIntensityListMapByTime" resultType="java.util.HashMap">
        SELECT value, DATE_FORMAT(data_time, '%H:%i:%s') as time
        FROM iot_light_intensity
        WHERE data_time BETWEEN #{startTime} AND #{endTime}
        ORDER BY data_time ASC
    </select>
    <select id="selectLatestIotLightIntensityMap" resultType="java.util.HashMap">
        SELECT value, DATE_FORMAT(data_time, '%H:%i:%s') as time
        FROM iot_light_intensity
        ORDER BY data_time DESC
        LIMIT 1
    </select>
</mapper> 